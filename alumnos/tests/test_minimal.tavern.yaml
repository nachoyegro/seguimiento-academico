---
# Every test file has one or more tests...
test_name: GET alumnos 1502

# ...and each test has one or more stages (e.g. an HTTP request)
stages:
  - name: Trae el ID correcto

    # Define the request to be made...
    request:
      url: "http://127.0.0.1:8000/api/alumnos/1502/"
      method: GET

    # ...and the expected response code and body
    response:
      status_code: 200
      body:
        id: 1502

---
test_name: Post a lot of data

stages:
  - name: Crear un nuevo alumno
    request:
      url: "http://127.0.0.1:8000/api/alumnos/"
      method: POST
      json: !include alumno_test.json
    response:
      status_code: 201
